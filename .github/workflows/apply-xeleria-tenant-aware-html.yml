name: Apply XelerIA tenant-aware HTML
# Disabled auto-run: manual only to avoid failing workflow email spam.

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled. Historical HTML patch should not auto-run."
